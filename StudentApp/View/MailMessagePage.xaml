<Page x:Class="StudentApp.View.MailMessagePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:StudentApp.View"
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="700"
      Title="MailMessagePage">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition ></RowDefinition>
            <RowDefinition Height="100"></RowDefinition>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1" Orientation="Horizontal">
            <Label Margin="10" x:Name="SubjectLabel"></Label>
            <Label Margin="10" x:Name="FromLabel"></Label>
            <Label Margin="10" x:Name="DateLabel"></Label>
           
        </StackPanel>
        <WebBrowser Grid.Row="0" x:Name="BodyWebBrowser"></WebBrowser>
    </Grid>
</Page>
